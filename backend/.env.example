# =============================================================================
# GlossFlow API - ambiente local/produção
# Copie este arquivo para .env e preencha com dados reais.
# Nunca versionar .env com credenciais reais.
# =============================================================================

# Banco MongoDB Atlas
DATABASE_URL="mongodb+srv://usuario:senha@cluster.mongodb.net/glossflow?retryWrites=true&w=majority"

PORT=3333
NODE_ENV="development"
FRONTEND_ORIGIN="http://localhost:5173"
PUBLIC_API_URL="http://localhost:3333"
APP_PUBLIC_URL="http://localhost:5173"

# Site white-label / multi-tenant
# Ex.: glossflow.com.br permite ana.glossflow.com.br, bella.glossflow.com.br etc.
PUBLIC_ROOT_DOMAIN=""
# Fallback usado no domínio de demonstração quando nenhum tenant é informado.
DEFAULT_PUBLIC_SALON_SLUG="glossflow"

# Segurança / sessão
JWT_SECRET="troque-por-uma-chave-forte-com-mais-de-32-caracteres"
ACCESS_TOKEN_MINUTES=30
REFRESH_TOKEN_DAYS=7
# Refresh tokens são rotacionados e access tokens são vinculados à UserSession.

# Rate limit por superfície/IP e por tenant autenticado
RATE_LIMIT_PER_MINUTE=180
AUTH_LOGIN_RATE_LIMIT_PER_MINUTE=12
AUTH_REFRESH_RATE_LIMIT_PER_MINUTE=60
PUBLIC_WRITE_RATE_LIMIT_PER_MINUTE=90
WEBHOOK_RATE_LIMIT_PER_MINUTE=600
TENANT_RATE_LIMIT_PER_MINUTE=600
TENANT_SECURITY_RATE_LIMIT_PER_MINUTE=30

# Retenção de dados (Marco 23)
# Sessões expiradas/revogadas mais antigas que este período são removidas.
SESSION_RECORD_RETENTION_DAYS=30
# Conteúdo antigo de WhatsApp é redigido, preservando apenas metadados mínimos.
WHATSAPP_CONTENT_RETENTION_DAYS=180
# Auditoria possui retenção maior para rastreabilidade operacional.
AUDIT_LOG_RETENTION_DAYS=730
BACKUP_METADATA_RETENTION_DAYS=180

# Backup lógico assinado / restore controlado
# Em produção recomenda-se segredo dedicado >= 32 chars e estável ao longo da vida
# dos snapshots. Se vazio, o backend usa JWT_SECRET apenas para assinar backups.
BACKUP_SIGNING_SECRET=""
# Kill switch. Manter false na operação normal. Habilitar somente durante runbook
# de recuperação e voltar para false imediatamente após o restore.
BACKUP_RESTORE_ENABLED="false"

# Super Admin da plataforma
# O startup pode provisionar/garantir esse usuário de forma idempotente.
SUPER_ADMIN_NAME="Super Admin GlossFlow"
SUPER_ADMIN_EMAIL=""
SUPER_ADMIN_PASSWORD=""

# Ciclo SaaS por tenant
# Se o Super Admin criar um TRIAL sem data, o backend calcula este prazo.
SAAS_DEFAULT_TRIAL_DAYS=7
# PAST_DUE pode manter uma janela de graça. Depois dela, login, sessão e operação
# do tenant são bloqueados até reativação pelo Super Admin.
SAAS_PAST_DUE_GRACE_DAYS=3
# O perfil de billing salvo no Super Admin registra provider e IDs comerciais,
# mas NÃO cria cobrança externa automaticamente.

# IA - Groq (principal)
AI_PROVIDER="groq"
GROQ_API_KEY=""
GROQ_MODEL="openai/gpt-oss-120b"
# Fallback opcional OpenAI
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4o-mini"

# WhatsApp
# Providers suportados: twilio | meta | provider HTTP customizado.
# A configuração operacional atual usa Twilio.
WHATSAPP_PROVIDER="twilio"
WHATSAPP_DRY_RUN="true"
# DDI padrão aplicado quando o cliente informa somente DDD + número.
WHATSAPP_DEFAULT_COUNTRY_CODE="55"
# Uma mutação proposta pela IA expira se o cliente não confirmar nesse período.
WHATSAPP_ACTION_CONFIRMATION_TTL_MINUTES=10

# Twilio WhatsApp
# Nunca compartilhe TWILIO_AUTH_TOKEN em prints, logs ou commits.
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_WHATSAPP_FROM="whatsapp:+14155238886"
# URL pública EXATA cadastrada em "When a message comes in" no Sandbox/Sender.
TWILIO_WEBHOOK_URL=""
# Callback de entrega. Se vazio, o backend deriva de TWILIO_WEBHOOK_URL ou PUBLIC_API_URL.
TWILIO_STATUS_CALLBACK_URL=""
# No Sandbox existe um sender compartilhado; associe-o ao tenant de QA.
# Se vazio, usa DEFAULT_PUBLIC_SALON_SLUG.
TWILIO_DEFAULT_SALON_SLUG=""
# Trial serve somente para QA e aceita o ContentSid fornecido pelo painel Try out WhatsApp.
TWILIO_TRIAL_MODE="false"
TWILIO_TRIAL_CONTENT_SID=""

# Meta WhatsApp Cloud API - provider alternativo, não necessário no fluxo Twilio.
WHATSAPP_API_VERSION="v20.0"
WHATSAPP_ACCESS_TOKEN=""
WHATSAPP_PHONE_NUMBER_ID=""
WHATSAPP_WEBHOOK_VERIFY_TOKEN=""
WHATSAPP_APP_SECRET=""

# Templates/Contents Utility do provider ativo.
# Twilio em produção: use o ContentSid HX... do template correspondente.
# Meta: use o nome do template aprovado.
# O Marco 20 usa estes identificadores quando a janela de atendimento está fechada.
WHATSAPP_TEMPLATE_LANGUAGE="pt_BR"

# confirmação - corpo com 7 parâmetros, nesta ordem:
# {{1}} cliente | {{2}} serviço | {{3}} data/hora | {{4}} profissional |
# {{5}} antecedência mínima | {{6}} URL de gerenciamento | {{7}} salão
WHATSAPP_TEMPLATE_APPOINTMENT_CONFIRMED=""

# cancelamento - corpo com 5 parâmetros:
# {{1}} cliente | {{2}} serviço | {{3}} data/hora | {{4}} profissional | {{5}} salão
WHATSAPP_TEMPLATE_APPOINTMENT_CANCELLED=""

# lembrete 24h - corpo com 6 parâmetros:
# {{1}} cliente | {{2}} salão | {{3}} serviço | {{4}} data/hora |
# {{5}} profissional | {{6}} antecedência mínima de cancelamento
WHATSAPP_TEMPLATE_APPOINTMENT_REMINDER_24H=""

# lembrete 2h - corpo com 5 parâmetros:
# {{1}} cliente | {{2}} serviço | {{3}} data/hora | {{4}} profissional | {{5}} salão
WHATSAPP_TEMPLATE_APPOINTMENT_REMINDER_2H=""

# Retenção / CRM. Fora da janela de atendimento, o envio é bloqueado se o
# identificador correspondente não estiver configurado.
WHATSAPP_TEMPLATE_RETENTION_BIRTHDAY=""
WHATSAPP_TEMPLATE_RETENTION_INACTIVE=""
WHATSAPP_TEMPLATE_RETENTION_FREQUENT=""
WHATSAPP_TEMPLATE_RETENTION_FOLLOWUP=""

# Provider HTTP personalizado opcional
WHATSAPP_API_URL=""
WHATSAPP_API_TOKEN=""

# Política da agenda
CLIENT_CANCELLATION_MIN_HOURS=12
BOOKING_SLOT_INTERVAL_MINUTES=30
APPOINTMENT_REMINDER_SCAN_MINUTES=10
BUSINESS_TIMEZONE="America/Sao_Paulo"
BUSINESS_TIMEZONE_OFFSET="-03:00"

# Pagamentos
PAYMENTS_DRY_RUN="true"
MERCADO_PAGO_ACCESS_TOKEN=""
DEFAULT_PAYER_EMAIL="cliente@example.com"
STRIPE_SECRET_KEY=""
STRIPE_PRICE_ID=""
STRIPE_SUCCESS_URL="http://localhost:5173/sucesso"
STRIPE_CANCEL_URL="http://localhost:5173/cancelado"

# Observabilidade / performance
# Acima deste tempo a API registra a rota como slow request.
OBSERVABILITY_SLOW_REQUEST_MS=750
# SLO usado pelo painel global do Super Admin.
OBSERVABILITY_SLO_P95_MS=750
OBSERVABILITY_SLO_ERROR_RATE_PCT=2
OBSERVABILITY_DEPENDENCY_SUCCESS_RATE_PCT=98
# createIndexes idempotente no bootstrap. Defina false apenas para diagnóstico.
SYNC_MONGO_INDEXES="true"
# Integração externa opcional. Sem SDK/DSN, logs e /metrics continuam funcionando.
SENTRY_DSN=""
VITE_SENTRY_DSN=""

# Integrações futuras/opcionais
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
CLOUDINARY_URL=""
META_ACCESS_TOKEN=""

# Comercial
DEFAULT_SALON_ID=""
COMMERCIAL_NOTIFY_WHATSAPP_TO=""
AFFILIATE_COMMISSION_PERCENT=20
TRIAL_DAYS=7