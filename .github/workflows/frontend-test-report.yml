name: Frontend Test Report

on:
  push:
    branches: [agent/rebuild-hygiene-m2-9]
    paths:
      - .github/workflows/frontend-test-report.yml

permissions:
  contents: write

jobs:
  test-report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/rebuild-hygiene-m2-9
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20.19.0
      - working-directory: frontend
        run: npm ci
      - name: Capture Vitest output
        working-directory: frontend
        run: npm test > ../frontend-test-report.txt 2>&1 || true
      - name: Commit report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add frontend-test-report.txt
          git commit -m "chore: capture frontend test report"
          git push origin HEAD:agent/rebuild-hygiene-m2-9
