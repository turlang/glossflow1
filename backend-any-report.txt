backend/src/routes/admin-crud.routes.ts:52: const updateData: any = { name: data.name, email: data.email, role: data.role, active: data.active };
backend/src/routes/admin-crud.routes.ts:175: }) as any);
backend/src/routes/admin-crud.routes.ts:178: await prisma.$transaction(operations as any);
backend/src/routes/analytics.routes.ts:55: const appointments = appointmentsRaw as any[];
backend/src/routes/analytics.routes.ts:56: const financialEntries = financialEntriesRaw as any[];
backend/src/routes/analytics.routes.ts:57: const clients = clientsRaw as any[];
backend/src/routes/analytics.routes.ts:58: const inventory = inventoryRaw as any[];
backend/src/routes/analytics.routes.ts:59: const professionals = professionalsRaw as any[];
backend/src/routes/analytics.routes.ts:60: const services = servicesRaw as any[];
backend/src/routes/analytics.routes.ts:63: .filter((entry: any) => entry.type === 'REVENUE' && entry.referenceDate >= monthStart && entry.referenceDate < nextMonth)
backend/src/routes/analytics.routes.ts:64: .reduce((sum: number, entry: any) => sum + money(entry.amount), 0);
backend/src/routes/analytics.routes.ts:66: .filter((entry: any) => entry.type === 'EXPENSE' && entry.referenceDate >= monthStart && entry.referenceDate < nextMonth)
backend/src/routes/analytics.routes.ts:67: .reduce((sum: number, entry: any) => sum + money(entry.amount), 0);
backend/src/routes/analytics.routes.ts:68: const totalRevenue = financialEntries.filter((entry: any) => entry.type === 'REVENUE').reduce((sum: number, entry: any) => sum + money(entry.amount), 0);
backend/src/routes/analytics.routes.ts:70: const appointments90 = appointments.filter((item: any) => item.startTime >= last90);
backend/src/routes/analytics.routes.ts:71: const appointments30 = appointments.filter((item: any) => item.startTime >= last30);
backend/src/routes/analytics.routes.ts:72: const completed = appointments.filter((item: any) => ['COMPLETED', 'CONFIRMED'].includes(item.status));
backend/src/routes/analytics.routes.ts:73: const totalServiceRevenue = completed.reduce((sum: number, item: any) => sum + money(item.service?.price), 0);
backend/src/routes/analytics.routes.ts:76: const customerKey = (appointment: any) => appointment.clientId || appointment.clientPhone || appointment.clientName;
backend/src/routes/analytics.routes.ts:77: const clientFrequency = appointments.reduce<Record<string, number>>((acc: Record<string, number>, appointment: any) => {
backend/src/routes/analytics.routes.ts:87: const activeKeys30 = new Set(appointments30.map((appointment: any) => customerKey(appointment)).filter(Boolean));
backend/src/routes/analytics.routes.ts:88: const activeKeys90 = new Set(appointments90.map((appointment: any) => customerKey(appointment)).filter(Boolean));
backend/src/routes/analytics.routes.ts:101: const occupied = appointments.filter((item: any) => item.startTime >= monthStart && item.startTime < nextMonth).length;
backend/src/routes/analytics.routes.ts:104: const serviceRanking = Object.values(appointments.reduce<Record<string, any>>((acc: Record<string, any>, appointment: any) => {
backend/src/routes/analytics.routes.ts:110: }, {})).sort((a: any, b: any) => b.revenue - a.revenue).slice(0, 8);
backend/src/routes/analytics.routes.ts:112: const professionalRanking = Object.values(appointments.reduce<Record<string, any>>((acc: Record<string, any>, appointment: any) => {
backend/src/routes/analytics.routes.ts:118: }, {})).sort((a: any, b: any) => b.revenue - a.revenue).slice(0, 8);
backend/src/routes/analytics.routes.ts:120: const lowStock = inventory.filter((item: any) => Number(item.quantity || 0) <= Number(item.minimumQuantity || 0));
backend/src/routes/analytics.routes.ts:121: const inventoryValue = inventory.reduce((sum: number, item: any) => sum + money(item.costPrice) * Number(item.quantity || 0), 0);
backend/src/routes/audit.ts:13: const auditModel = (prisma as any).auditLog;
backend/src/routes/auth.routes.ts:42: return (prisma as any).userSession;
backend/src/routes/business.routes.ts:112: const projections = appointments.map((appointment: any) => {
backend/src/routes/business.routes.ts:113: const rule = rules.find((item: any) => item.professionalId === appointment.professionalId && item.active);
backend/src/routes/business.routes.ts:237: const revenue = financialEntries.filter((entry: any) => entry.type === 'REVENUE').reduce((sum: number, entry: any) => sum + entry.amount, 0);
backend/src/routes/business.routes.ts:238: const expenses = financialEntries.filter((entry: any) => entry.type === 'EXPENSE').reduce((sum: number, entry: any) => sum + entry.amount, 0);
backend/src/routes/business.routes.ts:240: const lowStock = inventory.filter((item: any) => item.quantity <= item.minimumQuantity);
backend/src/routes/business.routes.ts:241: const appointmentsValue = appointments.reduce((sum: number, appointment: any) => sum + Number(appointment.service?.price || 0), 0);
backend/src/routes/business.routes.ts:243: const topService = [...services].sort((a: any, b: any) => Number(b.price || 0) - Number(a.price || 0))[0] as any;
backend/src/routes/business.routes.ts:252: answer = ['Análise de estoque:', `• Produtos monitorados: ${inventory.length}.`, `• Produtos em alerta: ${lowStock.length}.`, lowStock.length ? `• Prioridade: ${lowStock.slice(0, 3).map((item: any) => item.name).join(', ')}.` : '• Nenhum produto abaixo do mínimo agora.', '• Boa prática: vincular consumo de produto ao serviço para previsão automática.'].join('\n');
backend/src/routes/business.routes.ts:256: const ranking = professionals.map((professional: any) => {
backend/src/routes/business.routes.ts:257: const total = appointments.filter((appointment: any) => appointment.professionalId === professional.id).reduce((sum: number, appointment: any) => sum + Number(appointment.service?.price || 0), 0);
backend/src/routes/business.routes.ts:259: }).sort((a: any, b: any) => b.total - a.total);
backend/src/routes/business.routes.ts:260: answer = ['Análise por profissional:', ...ranking.slice(0, 5).map((item: any, index: number) => `• ${index + 1}. ${item.name}: R$ ${item.total.toFixed(2)} em agenda.`), '• Recomendação: compare ocupação, ticket médio e recorrência antes de definir metas.'].join('\n');
backend/src/routes/business.routes.ts:267: const context = { services: services.length, professionals: professionals.length, appointments: appointments.length, clients: clients.length, revenue, expenses, profit, averageTicket, lowStock: lowStock.map((item: any) => item.name).slice(0, 8) };
backend/src/routes/business.routes.ts:277: const data = await aiResponse.json() as any;
backend/src/routes/business.routes.ts:299: const lowStock = inventory.filter((item: any) => item.quantity <= item.minimumQuantity);
backend/src/routes/business.routes.ts:300: const revenue = financialEntries.filter((e: any) => e.type === 'REVENUE').reduce((sum: number, e: any) => sum + e.amount, 0);
backend/src/routes/business.routes.ts:301: const expenses = financialEntries.filter((e: any) => e.type === 'EXPENSE').reduce((sum: number, e: any) => sum + e.amount, 0);
backend/src/routes/growth.routes.ts:56: const mrr = activeSubscriptions.reduce((sum: number, subscription: any) => sum + Number(subscription.plan?.price || 0), 0);
backend/src/routes/growth.routes.ts:57: const trialCount = subscriptions.filter((subscription: any) => subscription.status === 'TRIAL').length;
backend/src/routes/growth.routes.ts:58: const activeCount = subscriptions.filter((subscription: any) => subscription.status === 'ACTIVE').length;
backend/src/routes/growth.routes.ts:59: const pastDueCount = subscriptions.filter((subscription: any) => subscription.status === 'PAST_DUE').length;
backend/src/routes/growth.routes.ts:65: recentSubscriptions: subscriptions.slice(0, 8).map((subscription: any) => ({
backend/src/routes/growth.routes.ts:83: return salons.map((salon: any) => ({
backend/src/routes/growth.routes.ts:157: const segments = clients.map((client: any) => {
backend/src/routes/growth.routes.ts:159: const totalSpent = client.appointments.reduce((sum: number, appointment: any) => sum + Number(appointment.service?.price || 0), 0);
backend/src/routes/growth.routes.ts:161: const loyaltyPoints = client.loyaltyEntries.reduce((sum: number, entry: any) => sum + Number(entry.points || 0), 0);
backend/src/routes/growth.routes.ts:178: vip: segments.filter((client: any) => client.tags.includes('VIP')),
backend/src/routes/growth.routes.ts:179: inactive60Days: segments.filter((client: any) => client.tags.includes('INATIVO_60_DIAS')),
backend/src/routes/growth.routes.ts:180: birthdays: segments.filter((client: any) => client.tags.includes('ANIVERSARIANTE')),
backend/src/routes/growth.routes.ts:188: const inactive = clients.filter((client: any) => !client.appointments[0] || new Date(client.appointments[0].startTime) < daysAgo(60));
backend/src/routes/growth.routes.ts:189: const topClients = clients.filter((client: any) => client.appointments.reduce((sum: number, appt: any) => sum + Number(appt.service?.price || 0), 0) >= 500);
backend/src/routes/growth.routes.ts:207: return professionals.map((professional: any) => {
backend/src/routes/growth.routes.ts:208: const items = appointments.filter((appointment: any) => appointment.professionalId === professional.id);
backend/src/routes/growth.routes.ts:255: const revenue = financialEntries.filter((entry: any) => entry.type === 'REVENUE').reduce((sum: number, entry: any) => sum + Number(entry.amount || 0), 0);
backend/src/routes/growth.routes.ts:256: const expenses = financialEntries.filter((entry: any) => entry.type === 'EXPENSE').reduce((sum: number, entry: any) => sum + Number(entry.amount || 0), 0);
backend/src/routes/growth.routes.ts:257: const appointmentValue = appointments.reduce((sum: number, appointment: any) => sum + Number(appointment.service?.price || 0), 0);
backend/src/routes/growth.routes.ts:259: const lowStock = inventory.filter((product: any) => product.quantity <= product.minimumQuantity);
backend/src/routes/growth.routes.ts:261: const professionals = appointments.reduce((acc: Record<string, number>, appointment: any) => {
backend/src/routes/growth.routes.ts:275: professionalRanking: Object.entries(professionals).map(([name, total]) => ({ name, total })).sort((a: any, b: any) => b.total - a.total),
backend/src/routes/observability.routes.ts:6: const model = (name: string) => (prisma as any)[name];
backend/src/routes/observability.routes.ts:32: const revenue = financialEntries.filter((entry: any) => entry.type === 'REVENUE').reduce((sum: number, entry: any) => sum + Number(entry.amount || 0), 0);
backend/src/routes/observability.routes.ts:33: const expenses = financialEntries.filter((entry: any) => entry.type === 'EXPENSE').reduce((sum: number, entry: any) => sum + Number(entry.amount || 0), 0);
backend/src/routes/platform-admin.routes.ts:78: const mrr = activeSubscriptions.reduce((sum: number, subscription: any) => sum + Number(subscription.plan?.price || 0), 0);
backend/src/routes/platform-admin.routes.ts:83: trial: subscriptions.filter((item: any) => item.status === 'TRIAL').length,
backend/src/routes/platform-admin.routes.ts:84: active: subscriptions.filter((item: any) => item.status === 'ACTIVE').length,
backend/src/routes/platform-admin.routes.ts:85: pastDue: subscriptions.filter((item: any) => item.status === 'PAST_DUE').length,
backend/src/routes/platform-admin.routes.ts:86: canceled: subscriptions.filter((item: any) => item.status === 'CANCELED').length
backend/src/routes/platform-admin.routes.ts:88: recentSubscriptions: subscriptions.slice(0, 10).map((subscription: any) => ({
backend/src/routes/platform-admin.routes.ts:112: return salons.map((salon: any) => {
backend/src/routes/platform-admin.routes.ts:113: const owner = salon.users.find((user: any) => user.role === 'ADMIN') || salon.users[0] || null;
backend/src/routes/platform-admin.routes.ts:124: activeUsers: salon.users.filter((user: any) => user.active).length,
backend/src/routes/platform-admin.routes.ts:220: const revenue = financialEntries.filter((entry: any) => entry.type === 'REVENUE').reduce((sum: number, entry: any) => sum + Number(entry.amount || 0), 0);
backend/src/routes/platform-admin.routes.ts:221: const expenses = financialEntries.filter((entry: any) => entry.type === 'EXPENSE').reduce((sum: number, entry: any) => sum + Number(entry.amount || 0), 0);
backend/src/routes/platform-admin.routes.ts:222: const lowStock = products.filter((item: any) => Number(item.quantity) <= Number(item.minimumQuantity)).length;
backend/src/routes/professional-schedule.routes.ts:91: weeklySchedule: data.weeklySchedule as any
backend/src/routes/professional-schedule.routes.ts:114: await prisma.professional.update({ where: { id }, data: { timeBlocks: next as any } });
backend/src/routes/professional-schedule.routes.ts:128: await prisma.professional.update({ where: { id }, data: { timeBlocks: next as any } });
backend/src/routes/security.routes.ts:7: const model = (name: string) => (prisma as any)[name];
backend/src/services/public-booking-availability.service.ts:173: function publicProfessional(professional: any) {
backend/src/services/waitlist.service.ts:102: slot: any;
backend/src/services/whatsapp.service.ts:92: function metaErrorDetails(data: any) {
